# Ruby / Bundler — local install artifacts, regenerated by `bundle install`
vendor/
.bundle/
.ruby-version.bak

# Jekyll build output — regenerated by `jekyll build`; GitHub Pages builds from source server-side
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
